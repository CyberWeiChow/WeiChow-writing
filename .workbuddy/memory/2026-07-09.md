# 2026-07-09 — 传播力完善 + TOC Bug 修复 + GitHub 部署修复

## GitHub Pages 部署问题修复（核心问题）
- **根因**: `build.py` 输出 `portfolio.html`，但 GitHub Pages 默认服务 `index.html`
- **修复**: build.py 改为输出 `index.html`，同时 copy 一份 `portfolio.html` 供本地预览
- **Git 代理修复**: 沙箱代理 49183 返回 502，改为配置 git global proxy → `http://127.0.0.1:7890`（用户本机 Clash）
- **凭据**: 配置 `credential.helper=osxkeychain`，但 keychain 中无 GitHub 凭据，需要用户在终端手动 push 一次输入用户名/Token

## TOC 点击跳转 Bug 修复
- 问题：TOC 链接 `href="#sec-X"` 触发 hashchange → route() → 落入 showBio()
- 修复：① initTOCTracking 中给链接加 click handler + preventDefault + smooth scroll；② route() 开头加守卫 `if (h !== '#' && !h.startsWith('#/')) return;`

## 传播力三项功能

### 1. Favicon
- 使用 SVG data URI 内联「未」字 favicon（奶油底 + 墨色字）
- 同时设置 apple-touch-icon

### 2. SEO / Open Graph 标签
- `<head>` 中添加静态 OG 标签：og:title, og:description, og:image, og:type, og:locale
- 添加 Twitter Card 标签：twitter:card (summary_large_image), twitter:title, twitter:description, twitter:image
- `__OG_IMAGE__` 占位符在 Python 构建时替换为 site.photo data URL
- 新增 `updateMeta()` 函数：动态更新 document.title 和所有 OG/Twitter 标签
  - showRead: 更新为文章标题 + 副标题/摘要 + 封面图
  - showBio/showExperience/showWorks/showContact: 重置为站点默认值

### 3. 文末分享功能
- 在「— Fin —」之后添加分享栏（share-bar）
- 「复制链接」按钮：navigator.clipboard.writeText + execCommand fallback，复制后显示"已复制 ✓"2秒
- 「二维码」按钮：点击切换显示 QR code，使用 api.qrserver.com 生成当前页面 URL 的二维码
- QR 码白色背景容器，移动端缩小到 140px

## Experience 板块分组优化
- **删除**亚运会志愿者经历（manifest.json journey 数组）
- **分组**: `showExperience()` 将 journey 按 `type` 分成两组：
  - 教育经历 (Education): type === 'education'（UCASS 硕士、浙工大学士）
  - 工作与项目 (Work & Projects): type === 'work' || type === 'event'（阳狮、香港会议、浙商、浙工大助教）
- 新增 `groupHTML(title, sub, items, baseIdx)` 辅助函数生成分组
- 新增 CSS: `.j-group` / `.j-group-head` / `.j-group-title` / `.j-group-sub`，分组标题 sticky 吸顶 + 右侧细线分隔
- 移除卡片上的 `j-type` 徽章（已被分组标题取代，冗余）
- 地图 pins 逻辑不变（仍按 cityEn 关联卡片）

## 新增「新媒体」Media 板块
- 作品集 Keynote 中确认有「New Media Operation / 新媒体运营」与「Rednote Operation / 小红书运营」板块，但具体内容以图片嵌入，无法完整解析
- 在 `manifest.json` 新增 `media` 字段，包含 `accounts`（微信公众号、小红书账号信息）和 `posts`（帖子示例数据）
- 在导航中添加 **Media** 入口，位于 Works 与 Contact 之间
- 新增 `showMedia()` 视图函数，采用**与文字作品完全不同的视觉逻辑**：
  - 账号头图卡片（平台图标 + 账号名 + bio + followers）
  - 画廊式网格卡片（4:5 封面、平台徽章、标题、日期、指标）
  - 点击卡片打开**灯箱弹窗**展示大图 + 指标 + 原文链接
- 平台配色：微信绿 `#07C160`、小红书红 `#FF2442`
- 响应式：桌面 3 列，平板 2 列，手机账号卡片单列、画廊 2 列
- 当前为示例数据，用户需在 `manifest.json` 中替换真实标题、封面图、链接和指标
